# @vestige/mcp-darwin-x64

The release workflow extracts `vestige-mcp-x86_64-apple-darwin.tar.gz` into this directory before publishing. This placeholder exists so the package layout is valid in the repository.
