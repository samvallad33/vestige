# @vestige/mcp-linux-x64

The release workflow extracts `vestige-mcp-x86_64-unknown-linux-gnu.tar.gz` into this directory before publishing. This placeholder exists so the package layout is valid in the repository.
