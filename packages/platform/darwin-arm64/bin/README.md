# @vestige/mcp-darwin-arm64

The release workflow extracts `vestige-mcp-aarch64-apple-darwin.tar.gz` into this directory before publishing. This placeholder exists so the package layout is valid in the repository.
