# @vestige/mcp-linux-arm64

The release workflow extracts `vestige-mcp-aarch64-unknown-linux-gnu.tar.gz` into this directory before publishing. This placeholder exists so the package layout is valid in the repository.
