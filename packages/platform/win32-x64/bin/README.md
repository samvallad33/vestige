# @vestige/mcp-win32-x64

The release workflow extracts `vestige-mcp-x86_64-pc-windows-msvc.zip` into this directory before publishing. This placeholder exists so the package layout is valid in the repository.
